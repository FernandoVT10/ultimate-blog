tags: [name: tag]
paths:
  /tags:
    get:
      tags: [tag]
      description: Returns all tags
      responses:
        200:
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Tag"
